# Gate 8 HERO patch

Copy this patch into the existing `Liaho` repository root, preserving the folder structure and allowing the listed source files to replace their Gate 7 placeholders.

Do not delete existing `public/` assets.

After upload run the validation sequence in `docs/GATE8_HERO.md`.
